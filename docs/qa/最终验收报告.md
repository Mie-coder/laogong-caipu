# 老公菜谱 Stitch V3 最终验收报告

> 状态：**通过**
>
> 验收日期：2026-07-19
>
> 验收原则：Critical / Important 问题必须退回修正；只有规格、质量、自动化、三宽度视觉与人工复核均有实际证据时才能通过。

## 1. 最终结论

| 验收项 | 结果 | 最终证据 |
| --- | --- | --- |
| 规格符合性 | PASS | Task 1–6 均经独立 reviewer 复审；Task 6 最终为 Spec PASS |
| 代码质量 | APPROVED | Task 6 最终复审 0 Critical / 0 Important / 0 Minor |
| ESLint | 通过 | `npm run lint`，exit 0，0 errors，12 warnings |
| 完整单元测试 | 通过 | 22 files / 126 tests，exit 0 |
| Production build | 通过 | Next.js 14.2.35，编译/类型检查通过，9/9 静态页生成 |
| Playwright 全流程 | 通过 | 三宽度 12/12；视觉修正后三宽主流程 3/3；375px 证据顺序修正后 1/1 |
| 375 / 390 / 430 视觉 | 通过 | 30 张最终截图，每宽度 10 张，文件名和尺寸全部核对 |
| Apple 动效与降级 | 通过 | 统一 spring、按压反馈、Vaul Drawer、Reduced Motion / Transparency 降级 |
| 无障碍与安全区 | 通过 | 44px 命中区、键盘删除、ARIA 状态、fixed UI 遮挡与横向溢出断言 |

**最终结论：通过。实现快照为 `a3f25ca`。**

## 2. 验收快照

- 工作树：`/Users/mie/.codex/worktrees/cf87/newAiTest`
- Git 状态：detached HEAD（用户已确认）
- 实现范围：`d882719..a3f25ca`
- Task 6 实现 commit：`a3f25ca test: complete Stitch V3 mobile acceptance`
- Node.js：`v22.23.1`（`package.json` 声明 `20.x`）
- npm：`10.9.8`
- 操作系统：macOS 12.7.6 (21H1320)
- Playwright：1.61.0
- Chromium：Google Chrome for Testing 149.0.7827.55
- 验收 AI Provider：`mock`
- 验收数据库：`/private/tmp/laogong-caipu-stitch-v3-e2e.sqlite`
- 保护文件：`DESIGN.md`、用户概念图和所有 `output/` 证据目录均未纳入实现 commit

## 3. 任务级审查记录

| Task | 主实现 commit | 最终规格 | 最终质量 | 验收摘要 |
| --- | --- | --- | --- | --- |
| 设计 token、shadcn/ui 与应用壳 | `d882719` | PASS | APPROVED | 经 `416c46b`、`d86c574` 返工后通过 |
| 首页、列表与导航 | `98db6f6` | PASS | APPROVED | 三宽度 Home/List、FAB、导航、滚动与无溢出通过 |
| 导入、解析、图片审核与确认 | `8b05064` | PASS | APPROVED | 修正 commits：`005f30d`、`f599b55`、`b02386b` |
| 详情、收藏与复盘 | `48b8199` | PASS | APPROVED | 修正 commits：`fa74337`、`4c86043`、`ab59bce`、`214a5ac` |
| 做菜指引、计时、步骤与语音 | `07b5bd7` | PASS | APPROVED | 真实会话恢复、绝对 deadline 计时、步骤撤销、语音降级通过 |
| 三宽度总验收与证据 | `a3f25ca` | PASS | APPROVED | 126/126、build 9/9、E2E 12/12 + 最终主流程 3/3，30 图通过 |

## 4. 自动化验收

### 4.1 ESLint

```bash
npm run lint
```

- 结果：exit 0，0 errors，12 warnings。
- 9 条为用户/动态图片使用原生 `<img>` 的 Next.js 优化提示；3 条为已有 effect 依赖提示。
- 没有通过关闭规则、`ts-ignore` 或 blanket ignore 隐藏错误。

### 4.2 定向与完整单元测试

```bash
npm run test -- tests/unit/import-flow-v3.test.tsx
npm run test -- tests/unit/recipe-list-v3.test.tsx
npm run test
```

- 导入流程：26/26，包括从解析结果继续图片审核且不重复 parse。
- 列表：16/16，包括返回时先恢复搜索栏布局，再精确恢复滚动位置。
- 最终全量：22 files / 126 tests，exit 0。
- 全量测试后的最后修改只涉及 cooking CSS 与 E2E 截图顺序，不改动业务 TypeScript。

### 4.3 Production build

```bash
npm run build
```

- 结果：exit 0。
- Next.js 14.2.35 编译、ESLint/类型检查、page data 收集、9/9 静态页生成和 build traces 全部成功。
- 路由包含 `/`、`/recipes`、`/recipes/[id]`、`/recipes/[id]/cook` 及全部 API 路由。

### 4.4 Playwright

```bash
AI_PROVIDER=mock DATABASE_PATH=/private/tmp/laogong-caipu-stitch-v3-e2e.sqlite npm run test:e2e
```

- 最终全套：12/12，375 / 390 / 430，1 worker，5.5m。
- 覆盖：导入至复盘主流程、搜索/分类/删除/返回滚动、parse 拒绝保留输入、图片 fallback 不重复 parse、Stitch V3 结构契约。

人工验图发现长步骤和食材布局后，修正了业务 CSS 并增加几何断言，然后执行：

```bash
AI_PROVIDER=mock DATABASE_PATH=/private/tmp/laogong-caipu-stitch-v3-e2e.sqlite \
  npx playwright test tests/e2e/mobile-flow.spec.js --grep "imports, favorites, cooks"
```

- 最终三宽度主流程：3/3，4.5m。
- 375px 截图恢复顶部顺序后：1/1，2.0m。
- 最终断言包括：每宽度恰好 10 张规定截图、无页面横向溢出、fixed footer 不挡内容、Drawer 末项可达、长步骤不越界/不与下一项重叠、食材子元素不越出按钮。

## 5. 十屏三尺寸视觉验收

最终证据根目录：`output/playwright-stitch-v3/`。该目录按用户要求保留在工作树，不纳入 Git commit。

| # | 屏幕 | 截图名 | 375 | 390 | 430 | Stitch 对照 |
| --- | --- | --- | --- | --- | --- | --- |
| 01 | 首页-导航优化 | `01-home-nav.png` | 通过 | 通过 | 通过 | 标题、Hero、导入入口、最近做过与底栏层级一致 |
| 02 | 菜谱列表 | `02-recipe-list.png` | 通过 | 通过 | 通过 | 分段、chips、圆图、FAB、底栏一致 |
| 03 | 导入抽屉 | `03-import-sheet.png` | 通过 | 通过 | 通过 | 遮罩、拖拽柄、输入和主操作一致 |
| 04 | 解析进度 | `04-parsing-progress.png` | 通过 | 通过 | 通过 | 来源卡、四阶段时间线和取消操作一致 |
| 05 | 图片审核 | `05-image-review.png` | 通过 | 通过 | 通过 | 主图、封面、缩略图与双操作底栏一致 |
| 06 | 菜谱确认编辑 | `06-recipe-confirm.png` | 通过 | 通过 | 通过 | 可编辑元数据、食材、步骤与 fixed 保存一致 |
| 07 | 菜谱详情 | `07-recipe-detail.png` | 通过 | 通过 | 通过 | 大图、元数据、Tabs、复盘/做菜底栏一致 |
| 08 | 进入做菜指引 | `08-cooking-guide.png` | 通过 | 通过 | 通过 | 全高 Drawer、成品图、提示和确认入口一致 |
| 09 | 做菜模式 | `09-cooking-mode.png` | 通过 | 通过 | 通过 | 食材轨、步骤、语音、计时和 fixed footer 一致；长文不重叠 |
| 10 | 菜谱复盘抽屉 | `10-recipe-review-sheet.png` | 通过 | 通过 | 通过 | 五星、评价、改进 chips 和保存底栏一致 |

尺寸核对：

- `375/`：10 张，全部 375 × 812。
- `390/`：10 张，全部 390 × 844。
- `430/`：10 张，全部 430 × 932。
- 390px 与 `.stitch/designs/01..10` 逐屏人工对照；375 / 430 额外检查响应式、长文、安全区和 fixed UI。

## 6. 真实交互验收矩阵

| 功能 | 结果 | 证据 |
| --- | --- | --- |
| 导入抽屉 | 通过 | 空内容禁用、粘贴保留、Drawer 语义与关闭 |
| 解析 | 通过 | 四阶段、取消确认、422 保留输入、失败可重试 |
| 图片审核 | 通过 | 选择/取消、封面、无图保存、filter fallback、返回不重复 parse |
| 确认编辑 | 通过 | 标题、标签、食材、步骤编辑/验证/保存与失败保留 |
| 搜索/分类/删除 | 通过 | URL 状态、服务端过滤、键盘确认删除、返回精确恢复滚动 |
| 收藏 | 通过 | 详情、列表、做菜模式状态一致；PATCH 完成后刷新仍保留 |
| 做菜指引 | 通过 | 必须显式确认才进入第 1 步 |
| 计时 | 通过 | 增减、开始、暂停、继续、结束与绝对 deadline 恢复 |
| 步骤 | 通过 | 完成、撤销、重新完成、计数、恢复；长步骤不重叠 |
| 语音 | 通过 | `zh-CN` 播报、切步 cancel、不支持/同步异常降级 |
| 复盘 | 通过 | 五星、评价、改进 chips/文字、保存后做过次数与评分刷新 |

## 7. Apple 动效验收

| 检查项 | 结果 | 实现/证据 |
| --- | --- | --- |
| 统一弹簧 | 通过 | `appleSpring = { stiffness: 420, damping: 38, mass: 0.8 }` |
| 导航选中态 | 通过 | Framer Motion 共享 `layoutId="v3-nav-active"` |
| 即时按压反馈 | 通过 | 业务操作使用 `data-press-feedback="apple"`，active 为 0.98 scale |
| Drawer 拖拽 | 通过 | 复用 shadcn/ui Drawer + Vaul，不自造通用手势引擎 |
| Reduced Motion | 通过 | 共享 indicator 改为 0.01s；press 取消 scale；Drawer/详情/做菜 transition 降至 1ms |
| Reduced Transparency | 通过 | 导航、Drawer footer、指引和做菜 fixed UI 降级为不透明 surface，移除 blur |

Playwright 三宽度主流程使用 `reducedMotion: "reduce"` 验证降级路径；正常动效路径由代码复审、Task 2–5 真浏览器验图和 Vaul/Framer Motion 原语约束验证。

## 8. shadcn/ui 与组件约束

- 通用交互组件已使用：Button、Input、Textarea、Label、Checkbox、Tabs、Drawer、Dialog、AlertDialog、DropdownMenu、Skeleton、Sonner。
- Drawer 底层使用 Vaul；Dialog/Tabs/Checkbox/Menu 底层使用 Radix 原语。
- 业务组件仅组合通用原语：ImportFlow、ImageCarousel、RecipeCard/List/Detail、FavoriteButton、CookingLogSheet、CookingMode。
- 图片、内容列表、时间线和业务版式没有对应通用 shadcn 原语，因此保留为业务 CSS/组件。
- 图标统一使用 Lucide；没有为按钮、抽屉、对话框等从零重造通用组件。

## 9. 无障碍、响应式与安全区

- shadcn 基础交互命中目标最小 44px；基础契约测试覆盖 Button/Input/Textarea/Checkbox/Tabs。
- 图标按钮有 accessible name；收藏、星级、chips 和步骤有 `aria-pressed`、`aria-current`、Checkbox 等状态。
- 加载、计时、错误和保存结果使用 status/live 语义。
- 删除流程真浏览器验证焦点 + Enter；AlertDialog 负责二次确认。
- 三宽度均断言 `scrollWidth <= innerWidth`。
- 详情、确认、做菜和复盘 fixed UI 均断言 footer 留在 viewport，末尾内容可滚动到 footer 上方。

## 10. 失败退回与重验记录

本项目不将失败项降级为“已知差异”。下列问题均在当前 Task 停止通过、退回实现、定向重验并由 reviewer 复审：

- Task 1：Reduced Motion、Vaul 降级、44px 命中区、destructive token 与 Checkbox 选中态，修正为 `416c46b`、`d86c574`。
- Task 3：安全源图 fallback、封面往返、确认页验证、标签重命名/删除、缩略图语义，修正为 `005f30d`、`b02386b`。
- Task 4：死控件、404/可重试错误、返回/删除 fallback、favorite 类型归一化、Drawer 高度与 44px 命中区，修正为 `fa74337`、`4c86043`、`ab59bce`、`214a5ac`。
- Task 5：storage 异常、真实 steps hydrate、重复完成集合、无行为按钮、计时 footer 过宽、speech 同步异常，全部在 `07b5bd7` 前收口。
- Task 6：图片审核返回后无“继续审核”入口，增加 resume 事件与 26/26 回归。
- Task 6：列表返回滚动由 212px 夹到 140px。根因为搜索栏展开状态未恢复导致页面缩短；修正为先恢复 `searchOpen`，下一帧再恢复 scroll，严格 `before - 2` 断言通过。
- Task 6：初次 lint 因缺 ESLint config 直接 exit 2；添加标准 `next/core-web-vitals` 配置后 0 errors。
- Task 6：初次 build 发现 RAF callback 中 `scrollY` 类型收窄丢失；在 guard 后捕获局部 number，16/16 定向与 build 9/9 重验通过。
- Task 6：人工验图发现 cooking 长步骤与下一项重叠、长食材份量越界、review 截图滚过星级。退回修正业务 Button 几何、新增像素断言并重截三宽度。
- 最终 390px 主流程曾在已通过的详情→列表导航处出现一次本地 Next dev 超时；未改代码/超时/断言原样重跑为 1/1 通过，随后三宽度 3/3 通过。

## 11. 非阻塞说明

- **无未接受的阻塞差异。**
- E2E 使用固定 SVG 图片和 mock AI，用于可重复验证布局与业务状态；真实运行时依旧使用导入图片和所选 AI Provider。
- 12 条 lint warning 已在第 4.1 节如实记录，不影响 exit code、类型检查、build 或当前交互正确性。
- 真实 API 数据的菜名、分类、评分和图片可变；验收对照的是信息架构、组件几何、状态和交互契约。

## 12. Claude Code 二次验收交接

### 12.1 准确快照

```bash
git switch --detach a3f25ca
git rev-parse --short HEAD   # 必须输出 a3f25ca
git status --short
git show --stat --oneline a3f25ca
git log --oneline -15
```

应验收的实现 commit：`a3f25ca`。截图是未跟踪验收产物，Claude Code 可重跑 E2E 生成。

### 12.2 安装与环境

```bash
npm ci
export AI_PROVIDER=mock
export DATABASE_PATH=/private/tmp/laogong-caipu-claude-acceptance.sqlite
```

项目声明 Node 20.x；本次原始验收环境为 Node 22.23.1 + npm 10.9.8。

### 12.3 可复制复验命令

```bash
npm run lint
npm run test
npm run build
AI_PROVIDER=mock DATABASE_PATH=/private/tmp/laogong-caipu-claude-acceptance.sqlite npm run test:e2e
```

预期结果：

- lint：0 errors（可见本报告记录的 12 warnings）。
- unit：22 files / 126 tests。
- build：Next.js 14.2.35，9/9 页面。
- E2E：12/12，并生成 `output/playwright-stitch-v3/{375,390,430}/`。

### 12.4 人工复验入口

```bash
AI_PROVIDER=mock DATABASE_PATH=./data/claude-manual.sqlite npm run dev -- --hostname 127.0.0.1
```

- 首页：`http://127.0.0.1:3000/`
- 列表：`http://127.0.0.1:3000/recipes`
- 必查流程：导入→解析→图片审核→确认保存→收藏刷新→做菜指引→计时/步骤/语音→五星复盘。
- 额外必查：搜索和分类、删除二次确认、从详情返回列表的滚动恢复、375px 长步骤和 fixed footer。
- 截图索引：`output/playwright-stitch-v3/{375,390,430}/` 三个最终目录共 30 张；根目录下的 `task2/` 是旧过程证据，不计入最终 30 图。

### 12.5 Claude Code 复验结果栏

Claude Code 二次验收尚未执行；该项是用户后续的独立复验，不影响本次主验收“通过”结论。复验时请记录执行环境、日期、命令原始结果和新发现问题。

## 13. 签发

- 主验收：Codex 主控
- 实现模式：Subagent-Driven，同时活跃代理不超过 4，符合用户“最多 5 个 agent”约束
- 最终 reviewer：Spec PASS / Quality APPROVED / 0 Critical / 0 Important / 0 Minor
- 主验收结论：**通过**

## 14. 2026-07-19 手机实测修订验收

### 14.1 验收范围与准确快照

本节验收的是 2026-07-19 用户反馈后的手机端修订，基线为 `a19522b`，完成实现后的准确产品快照为 `2990b39`。验收期间始终使用独立工作树，未纳入或覆盖用户已有的 `DESIGN.md`、`docs/ui-concepts/09-12` 与旧 `output/` 证据。

| 阶段 | 提交 | 内容 |
| --- | --- | --- |
| Task 1 | `da9d23f`, `59f64f7` | Micu 服务端生成、12 MiB 流式限流、PNG 校验、稳定缓存与受控 API |
| Task 2 | `0ea1852`, `488874d`, `2990b39` | 横向食材卡可见时懒生成、失败回退、完整卡片观察目标、Strict Mode cleanup 后替代请求 |
| Task 3 | `005f030`, `1a177d0` | 整行点击完成/撤销、划线、自动推进、计数、紧凑底栏、复盘分隔线移除 |
| Task 4 | `3a48687`, `2f06f9c`, `e1c9a1c` | 自托管中文字体、标题层级、解析节点、封面 pill、确认页密度与 E2E 契约 |
| 构建返修 | `82df8f3`, `843c606` | Next Route 合法 export 与 Web `Response` 字节类型兼容 |

所有 Task 均执行“实现 → 独立 reviewer → 未通过则退回原实现 Agent → 完整范围复审”。最终静态复审结果为 Spec PASS / Quality APPROVED；无未关闭的 Critical、Important 或 Minor finding。

### 14.2 当前 HEAD 的自动化证据

| 门禁 | 命令/范围 | 结果 |
| --- | --- | --- |
| 定向单元测试 | `import-flow-v3`, `cooking-session`, `cooking-mode`, `cooking-log-sheet`, `ingredient-image-service`, `ingredient-image-route` | **6 files / 63 tests，全部通过**；包含 Route export shape 与 React Strict Mode abort/replacement 回归 |
| 完整单元测试 | `npm test` | **24 files / 147 tests，全部通过**；最终分支签发前新鲜执行 |
| lint | `npm run lint` | **exit 0，0 errors，13 warnings**；warning 为现有 `<img>` 优化建议与 hook dependency 提示，未静默隐藏 |
| 生产构建 | `npm run build` | **exit 0**；Next.js 14.2.35 编译、类型检查、9/9 页面生成完成；两条新增图片 API route 均进入路由表 |
| 390px 主流程 | `mobile-390`, grep `imports, favorites, cooks` | **1/1 通过，2.1m**；Strict Mode 修复后重新生成 10 张最终截图 |
| 375px 主流程 | `mobile-375`, grep `imports, favorites, cooks` | **1/1 通过，2.1m**；Strict Mode 修复后重新生成 10 张最窄宽度截图 |
| 真实 Micu | 真实服务、首次生成、PNG GET、重复请求 | **通过**；详细证据见 14.4 |

390px 最终截图目录：`output/playwright-stitch-v3/390/`；375px 最终截图目录：`output/playwright-stitch-v3/375/`。本轮没有重复跑 430px，原因是修订目标明确为用户手机截图与 375/390 最窄宽度，且用户要求减少重复测试。

### 14.3 六项反馈逐项结论

| 用户反馈 | 结果 | 验收证据 |
| --- | --- | --- |
| 图片审核“封面图”未居中、标题偏大 | **PASS** | span 与 shadcn Button 共用 `.image-review-cover-pill`；源码 `inline-flex`，计算样式双轴居中；审核标题 24px；`05-image-review.png` 人工复核通过 |
| 解析标题偏大、步骤条未对齐 | **PASS** | 标题 24px；marker 36px、node 32px、连线 `left:18px/top:32px`；状态只作用 node，不再改变轨道；`04-parsing-progress.png` 直线和四节点对齐 |
| 确认页标题/标签过大、分钟换行拥挤 | **PASS** | 页面标题与菜名 24px，chip 13px/32px；封面 88px；时间组 `white-space: nowrap`，数值与“分钟”保持同行；确认页菜名计算字体为 Noto Serif SC Variable |
| 复盘“做菜时间”横线贯穿 | **PASS** | legacy `border-bottom` 被直接删除，time row/footer 的 `border-top` 均为 0；`10-recipe-review-sheet.png` 未见贯穿分隔线 |
| 做菜底栏按钮拥挤 | **PASS** | 375/390 都断言 `flex-wrap: nowrap` 和无横向溢出；计时/操作按钮保持 44×44；`09-cooking-mode.png` 显示完整单行按钮组 |
| 食材缺图、步骤只能勾选 | **PASS** | 食材卡进入/接近可视区才请求服务器生成图，失败保留首字占位；E2E 验证真实 POST body、图片 `src`、`complete` 与 `naturalWidth`；步骤无 Checkbox，整行 shadcn Button 点击后文字贯穿划线，计数由完成集合派生并自动推进 |

字体层级最终契约：仅首页和菜谱列表主标题为 32px；解析、图片审核、确认页标题为 24px；菜名 24px；区块标题 20px；做菜导航标题 18px；正文 16px；chip 13px。中文主字体为自托管 `@fontsource-variable/noto-serif-sc@5.2.10`，字体栈为 `"Noto Serif SC Variable", "Songti SC", STSong, serif`，未使用运行时 CDN。

### 14.4 真实 Micu 动态生成与缓存证据

真实冒烟使用进程级 `MICU_API_KEY`，没有写入 `.env`、源码、数据库、测试、日志或本报告；客户端只提交菜谱 ID、`kind` 与 `index`，食材名和固定提示词由服务器解析。

- 冒烟食材在请求前不存在缓存，证明首次请求不是旧缓存命中。
- 首次请求耗时 **47,012 ms**，返回 HTTP 200；随后 GET 返回 `image/png`。
- PNG 大小 **2,572,779 bytes**，8 字节 PNG signature 校验通过。
- 第二次同食材请求耗时 **174 ms**，返回与首次相同的 key 和 URL。
- 对应缓存文件数量始终为 **1**，证明重复请求复用稳定缓存，没有生成第二份文件。
- 上游参数固定为 `gpt-image-2`、`1024x1024`、`quality: low`；缓存键为 `sha256("v1|" + normalizedIngredientName)`。

### 14.5 失败退回与最终通过记录

本轮没有把失败项写成“已知差异”：

1. Task 1 初审发现外部响应先完整缓冲、损坏缓存不清理、POST 会静默接收额外 prompt；退回后增加流式硬上限、损坏缓存删除再生与 strict schema，15/15 通过后复审 PASS。
2. Task 2 初审发现观察目标是头像而非完整食材 Button；退回并增加 `IntersectionObserver.observe(target)` 回归测试，6/6 后复审 PASS。
3. Task 3 初审发现旧 `border-bottom` 仍会形成复盘贯穿线；直接删除 legacy 声明，20/20 后复审 PASS。
4. Task 4 两轮审查先后发现后置字号覆盖、动态图 E2E 过弱、确认页菜名绕过新字体；全部退回修复，最终 Spec PASS / Quality APPROVED。
5. 第一次最终 build 因 app route 导出测试 factory 失败；退回并把 handler factory 移到纯库模块，新增 Route export-shape 回归测试。
6. 第二次 build 继续暴露 Node Buffer 不符合 Web `BodyInit`；退回改为真实 `Uint8Array.from(png)` 字节副本，禁止类型断言绕过。第三次 build 完整 exit 0。
7. 第一次全分支最终审查发现 React Strict Mode 会 abort 首次食材图请求、但旧 `requestedRef` 阻止第二次 effect 建立替代请求；该轮结论明确为 FAIL，没有沿用旧 E2E 签发。退回 Task 2 后新增真实 `<React.StrictMode>` 回归：首个 promise 等待 abort 并 reject，replacement request 成功出图；修复范围复审 PASS，随后 63/63、390/375、lint 与 build 均在 `2990b39` 重新通过。

### 14.6 Claude Code 二次验收命令

Claude Code 应在准确快照执行；若本报告提交后 HEAD 已前移，以包含本节的报告提交为准，但产品实现不得早于 `2990b39`。

```bash
git rev-parse --short HEAD
git log --oneline -16
git status --short

npm ci
npm run test -- \
  tests/unit/import-flow-v3.test.tsx \
  tests/unit/cooking-session.test.ts \
  tests/unit/cooking-mode.test.tsx \
  tests/unit/cooking-log-sheet.test.tsx \
  tests/unit/ingredient-image-service.test.ts \
  tests/unit/ingredient-image-route.test.ts
npm run lint

npx playwright test tests/e2e/mobile-flow.spec.js \
  --project=mobile-390 --grep "imports, favorites, cooks"
npx playwright test tests/e2e/mobile-flow.spec.js \
  --project=mobile-375 --grep "imports, favorites, cooks"

npm run build
```

真实 Micu 复验前，请在安全的本地进程环境中预先注入变量；不要把值写进仓库或验收报告：

```bash
: "${MICU_API_KEY:?请先在安全环境注入 MICU_API_KEY}"
AI_PROVIDER=mock \
DATABASE_PATH=/private/tmp/laogong-caipu-claude-micu.sqlite \
npm run dev -- --hostname 127.0.0.1
```

然后创建包含至少一个食材的菜谱，调用 `POST /api/recipes/:id/ingredient-images` body `{"kind":"ingredient","index":0}`，GET 返回的 `imageUrl` 并校验 PNG signature；再次 POST 同一引用，应返回同一 key，且缓存不新增第二个文件。验收过程中不得打印 Authorization header、环境变量值或上游原始响应。

人工检查以下截图：

- `output/playwright-stitch-v3/390/04-parsing-progress.png`
- `output/playwright-stitch-v3/390/05-image-review.png`
- `output/playwright-stitch-v3/390/06-recipe-confirm.png`
- `output/playwright-stitch-v3/390/09-cooking-mode.png`
- `output/playwright-stitch-v3/390/10-recipe-review-sheet.png`
- `output/playwright-stitch-v3/375/09-cooking-mode.png`

### 14.7 本轮签发

- 手机实测修订结论：**PASS**
- 自动化门禁：**PASS**
- 真实 Micu 动态生成与缓存：**PASS**
- 六项用户反馈：**全部 PASS**
- 未关闭的 Critical / Important / Minor：**0 / 0 / 0**
- Strict Mode 修复范围复审：**Spec PASS / Quality APPROVED**
- 最终全分支复审：**Spec PASS / Quality APPROVED / Ready to merge Yes；Critical / Important / Minor 为 0 / 0 / 0**
- Claude Code 二次验收：**待用户后续独立执行，不替代本轮 Codex 主验收**

## 15. 2026-07-19 家庭共享与食材加载整体验收

本节的集成实现范围为 `022c88c..8ace285`，恰有以下 4 个路径：

- `docs/qa/最终验收报告.md`
- `package.json`
- `playwright.family.config.cjs`
- `tests/e2e/family-cooking-flow.spec.js`

随后类型修复范围 `8ace285..bd3dd6c` 恰为 `src/lib/auth/config.ts`；最终 review repair 继续关闭 2 个 Important finding。本节当前最终验证的产品快照明确为 **`ecaece2`**，报告提交不属于产品实现范围。用户的云服务器没有被修改；部署仍是后续独立步骤。

### 15.1 新鲜门禁证据

- 历史集成实现阶段 `022c88c..8ace285`：完整单测 **32 files / 252 tests 通过**，lint 为 **0 errors / 12 warnings**；该证据保留用于说明集成实现过程，不作为当前快照的最终计数。
- 最终主控证据（精确产品快照 `ecaece2`）：完整 `npm run test` **exit 0，32 files / 255 tests 通过**；`npm run lint` **exit 0，0 errors / 12 warnings**。
- 最终主控在 **Node 22 + mock/test-only 环境**执行受控 `npm run build`：**exit 0，12 warnings**。Next.js 14.2.35 的生成路由表为 **18 rows（包含 `/_not-found`）**，排除 `/_not-found` 则为 **17 rows**；表中明确包含 `/unlock`、`/api/auth/login`、`/api/auth/logout`，并显示 `ƒ Middleware 27.7 kB`。这是 Next 实际生成的路由表计数，不是源码路径盘点。
- 最终 review repair 在 `ecaece2` 关闭 2 个 Important finding；复审为 **Spec PASS / Quality APPROVED / Ready to merge**，未关闭项为 **0 Critical / 0 Important / 1 nonblocking Minor**。该 Minor 是：若 destructive restore 前的两次 stop 尝试都失败，错误处理可能把 app 已停止表述得过强。
- SQLite：数据库初始化契约启用 `foreign_keys=ON`、`journal_mode=WAL`、`busy_timeout=5000`；部署契约验证在线备份、每日 7 份、每周图片快照 4 份、predeploy 3 份及失效锁 fail-closed。
- 食材图：优化器契约为**可完整解码的 256×256 WebP，且不超过 512 KiB**，路由 MIME 为 `image/webp`；RIFF 声明边界、chunk 完整性、Sharp 严格解码和尺寸都必须通过后才允许进入 immutable cache。本轮未使用真实 AI 或真实缓存图，也未记录 base64 数据。

### 15.2 390px 集成流结果

配置 `playwright.family.config.cjs` 使用唯一临时 SQLite 路径、mock AI、固定测试密码的 scrypt hash、32 字节以上测试会话密钥、1 worker、390×844、reduced motion；没有读取或输出用户 API 密钥。

集成实现期间曾观察到沙箱拒绝 localhost 监听，以及 dev 首次 hydration/assets 较慢；这些仅是当次运行的 harness 观察，不是可独立复现的长期产品门禁。最终主控在 `ecaece2` 的精确 tracked archive 上执行时，第一次 escalated 临时 shell 解析到 **Node 18.15**，因此未进入 Playwright tests；这同样是 runtime-path/harness 证据，不是产品失败。随后显式使用 **Node 22** 的同一 focused 命令完成最终验证：

- **E2E：1/1 通过**（Chromium、390×844、1 worker、reduced motion）；单测用例耗时 **50.2s**，完整命令耗时 **1.4m**。
- 已验证匿名 `/recipes` 重定向、登录回 `/recipes`、在已认证浏览器上下文创建含“排骨、姜、白芝麻”的菜谱、食材图请求保持时的 `aria-busy=true`/骨架/无横向溢出、确定性图响应后的真实 `<img>` complete 和 `aria-busy=false`、全部勾选/取消全选的每项 `aria-pressed`、退出家庭后重新访问 `/recipes` 仍被锁定。
- 仅生成一张新截图：`output/playwright-family-sharing/390/family-cooking.png`（**41,315 bytes**）；主控已完成视觉检查，截图保持未跟踪。

### 15.3 部署状态（与本地功能验收分离）

`ecaece2` 上本地已测试的家庭认证、UI、前台恢复与 mock-AI 流程**通过**；这个结论不代表完整部署就绪。进程重启后的持久化、真实 Docker、反向代理、备份恢复演练、真实 AI 与运行时基础设施审计均未验证。公开部署就绪状态因此仍为**阻塞**，并且 Node 20 已 EOL、Next 14 不受支持；这些事项没有在本次本地验收中执行或“修复”。

### 15.4 Claude Code 复验命令（仅测试值）

以下命令从 `ecaece2` 的 tracked archive 建立唯一临时副本；复验在归档目录中运行，因此当前工作树里任何未跟踪或忽略的 `.env*` 都不会被加载。

```bash
set -Eeuo pipefail
node -e 'const major=Number(process.versions.node.split(".")[0]); if (major !== 22) { console.error("复验证据要求 Node 22"); process.exit(1) }'

VERIFY_ROOT="$(mktemp -d /private/tmp/laogong-caipu-family-verify.XXXXXX)"
git archive ecaece2 | tar -x -C "$VERIFY_ROOT"
cd "$VERIFY_ROOT"

unset MICU_API_KEY DEEPSEEK_API_KEY DEEPSEEK_MODEL
export AI_PROVIDER=mock
export DATABASE_PATH="$VERIFY_ROOT/family-e2e.sqlite"
export FAMILY_PASSWORD_HASH="$(node -e 'const {scryptSync}=require("node:crypto"); const salt=Buffer.from("family-e2e-salt!"); process.stdout.write(`scrypt$${salt.toString("base64url")}$${scryptSync("family-e2e-password",salt,64).toString("base64url")}`)')"
export FAMILY_SESSION_SECRET="$(node -e 'process.stdout.write(require("node:crypto").randomBytes(32).toString("base64url"))')"

npm ci
npm run test
npm run lint
npm run build
npm run test:e2e:family
```

若本机尚无 Playwright Chromium，仅在 `npm ci` 后额外执行一次 `npx playwright install chromium`，再运行 E2E。整个复验不提供任何真实 Micu/DeepSeek key，也不得打印 Cookie 或 session token。

Node 22 是上述最终证据实际使用的受控 verifier runtime。仓库当前过时的 `engines` 仍写为 `>=20.9 <21`，因此 `npm ci` 可能显示 engine warning；Node 22 验证不代表它已经成为经过验证的部署 runtime。运行时与框架迁移仍是公开部署 blocker。复验应保持已记录的快照与计数，并重新检查匿名重定向、解锁回列表、三项食材的骨架到真实图替换、全选切换、退出后重新访问 `/recipes` 仍锁定，以及仅生成一张 `output/playwright-family-sharing/390/family-cooking.png`。
