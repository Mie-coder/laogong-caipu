# 老公菜谱 Stitch V3 最终验收报告

> 状态：待执行
>
> 规则：只有所有必过项均有实际证据且结果通过，本文档才能将最终结论改为“通过”。任何必过项失败时，任务退回修正并重新验收。

## 1. 最终结论

| 项目 | 状态 | 证据 |
| --- | --- | --- |
| 规格符合性 | 待执行 | 待填写审查报告与 commit 范围 |
| 代码质量 | 待执行 | 待填写审查报告 |
| 定向测试 | 待执行 | 待填写命令与结果 |
| 完整单元测试 | 待执行 | 待填写命令与结果 |
| 生产构建 | 待执行 | 待填写命令与结果 |
| Playwright E2E | 待执行 | 待填写命令与结果 |
| 375px 视觉验收 | 待执行 | 待填写截图索引 |
| 390px 视觉验收 | 待执行 | 待填写截图索引 |
| 430px 视觉验收 | 待执行 | 待填写截图索引 |
| Apple 动效与降级 | 待执行 | 待填写检查记录 |
| 无障碍与安全区 | 待执行 | 待填写检查记录 |

**最终结论：待执行**

## 2. 验收快照

- 验收日期：待填写
- 分支：待填写
- 起始 commit：待填写
- 最终 commit：待填写
- Node 版本：待填写
- npm 版本：待填写
- 操作系统与浏览器版本：待填写
- 测试数据库：待填写
- AI Provider：待填写
- 工作区状态：待填写

## 3. 任务级审查记录

每个实现任务必须同时获得“规格符合”和“代码质量”两个通过结论。

| Task | 实现 commit | 规格审查 | 质量审查 | 修正 commit | 复审结果 |
| --- | --- | --- | --- | --- | --- |
| 设计 token、shadcn/ui 与应用壳 | `d882719` | PASS（第三轮） | APPROVED（第三轮） | `416c46b`, `d86c574` | 通过；主控 15 files / 85 tests + build 9/9 |
| 首页、列表与导航 | `98db6f6` | PASS | APPROVED（第二轮） | 包含于 `98db6f6` | focused 43/43、主控 103/103、build 9/9、审计无输出；最终六图、三宽度无横向溢出、390px Stitch 对照及列表滚动/固定栏复验通过 |
| 导入完整流程 | `8b05064` | PASS（最终轮） | APPROVED（最终轮） | `005f30d`, `f599b55`, `b02386b` | 25/25 定向、107/107 全量、build 9/9；四屏 390px 主控视觉通过；最终 0 Critical/Important |
| 详情、收藏与复盘 | `48b8199` | PASS（最终轮） | APPROVED（最终轮） | `fa74337`, `4c86043`, `ab59bce`, `214a5ac` | 22/22 定向、111/111 全量、build 9/9；详情/复盘 390px 及收藏刷新持久化通过；最终 0 Critical/Important/Minor |
| 做菜指引、计时、步骤与语音 | `07b5bd7` | PASS（最终轮） | APPROVED（最终轮） | 包含于 `07b5bd7` | 21 项最终相关测试均有通过证据；主控 123/123、build 9/9；指引/做菜/恢复/复盘四张 390px 证据通过；最终 0 Critical/Important/Minor |

## 4. 自动化验收

### 4.1 定向测试

```bash
PATH=/Users/mie/.hermes/node/bin:$PATH npm run test -- tests/unit/v2-shell.test.tsx tests/unit/api-client.test.ts tests/unit/home-v3.test.tsx tests/unit/recipe-list-v3.test.tsx
```

- 预期：全部通过
- 实际：Task 2 修正后 4 files / 43 tests 通过
- 失败与修正记录：第一轮 reviewer 的 Hook 顺序、长按 click、URL 状态、死按钮、API 错误边界、FAB、删除错误与 motion mock 问题均由 RED 用例复现并在第二轮关闭

Task 3 最终定向回归：

```bash
npm run test -- tests/unit/import-flow-v3.test.tsx
```

- 实际：1 file / 25 tests 通过
- 覆盖：状态机往返、取消、图片 fallback/选择/封面、确认页验证/保存/恢复、标签新增/重命名/去重/删除、shadcn 紧凑添加控件及无障碍选择语义

Task 4 最终定向回归：

```bash
npm run test -- tests/unit/recipe-repository.test.ts tests/unit/recipe-api-shapes.test.ts tests/unit/recipe-detail-v3.test.tsx tests/unit/cooking-log-sheet.test.tsx
```

- 实际：4 files / 22 tests 通过；视觉收口后 `recipe-detail-v3` + `cooking-log-sheet` 为 2 files / 16 tests 通过
- 覆盖：增量收藏 migration、目标状态 API、乐观更新/回滚、详情 Tabs、404/可重试错误、删除返回状态、typed 做菜边界、复盘失败保留/成功刷新、Apple press hook 与 Drawer 视觉契约

Task 5 最终定向回归：

```bash
npm run test -- tests/unit/cooking-session.test.ts tests/unit/cooking-timer.test.tsx tests/unit/speech-narration.test.tsx tests/unit/cooking-mode.test.tsx tests/unit/recipe-detail-v3.test.tsx
```

- 修正轮组合结果：5 files / 20 tests 通过；最后新增的语音同步异常降级由 `speech-narration` 3/3 单独通过，因此当前五个相关文件合计 21 项均有通过证据
- 覆盖：损坏/错菜谱/不可用 storage 降级、真实步骤 hydrate、完成集合去重、绝对 deadline 计时与后台校准、计时结束、语音替换/卸载/同步异常降级、指引确认、步骤撤销、完成后复盘刷新

### 4.2 完整单元测试

```bash
PATH=/Users/mie/.hermes/node/bin:$PATH npm run test
```

- 预期：全部测试通过
- 实际：Task 2 主控复跑通过
- 原始结果摘要：17 files / 103 tests，exit 0

Task 3 关闭时的主控全量证据：18 files / 107 tests 通过。该全量检查早于最终视觉/无障碍小修；小修所影响的导入流程在之后由 25/25 定向套件复验，未重复全量执行。

Task 4 最终主控全量证据：18 files / 111 tests 通过，exit 0。全量之后的视觉收口仅改动 semantic CSS 与难度中文映射，并由相关 16/16 定向套件复验。

Task 5 主控全量证据：22 files / 123 tests 通过，exit 0。其后仅补充 `speechSynthesis.speak()` 同步抛错的安全降级和对应测试，由 `speech-narration` 3/3 定向复验；未重复全量执行。

### 4.3 生产构建

```bash
PATH=/Users/mie/.hermes/node/bin:$PATH npm run build
```

- 预期：构建成功，所有页面生成成功
- 实际：Task 2 主控复跑通过
- 原始结果摘要：Next.js 14.2.35 编译与类型检查通过，9/9 静态页面生成，exit 0

Task 3 关闭时的主控构建证据同样通过：编译、类型检查及 9/9 页面生成。后续仅修改视觉、表单交互和 aria 语义，按用户确认的轻量模式以定向测试收口。

Task 4 最终 build：首次构建在 `getRecipeApi` 的 `isFavorite` 可选泛型边界失败；以 `ab59bce` 在 API client 源头归一化必填 boolean 后重验通过，Next.js 14.2.35 编译、类型检查、9/9 页面生成及 trace 收集全部 exit 0。

Task 5 最终 build：Next.js 14.2.35 编译、lint/typecheck、9/9 页面生成及 trace 收集全部 exit 0；构建路由包含动态 `/recipes/[id]/cook`。最后的语音降级修正只触及隔离 hook，并由定向测试与真浏览器无错误证据复验，未重复 build。

### 4.4 Playwright E2E

```bash
PATH=/Users/mie/.hermes/node/bin:$PATH AI_PROVIDER=mock DATABASE_PATH=/tmp/laogong-caipu-stitch-v3-e2e.sqlite npm run test:e2e
```

- 预期：375、390、430 三个项目全部通过
- 实际：待执行
- 原始结果摘要：待填写

## 5. 十屏三尺寸视觉验收

每个屏幕必须在 375、390、430 三个宽度验收，共 30 个必备截图。390px 与 Stitch 第三版做基准对照；375px 和 430px 检查响应式、安全区、固定栏和无横向溢出。

| 屏幕 | 375px | 390px | 430px | Stitch 对照结论 | 差异与修正 |
| --- | --- | --- | --- | --- | --- |
| 首页-导航优化 | 通过 | 通过 | 通过 | 通过 | `task2-final/home-{375,390,430}.png`；390px 对照确认标题、历史入口、Hero、导入操作、最近做过与底栏层级一致，三宽度均无横向溢出 |
| 菜谱列表 | 通过 | 通过 | 通过 | 通过 | `task2-final/list-{375,390,430}.png`；390px 对照确认标题、分段控件、筛选、96px 圆图、56px FAB 与底栏一致；真实 API 文案与排序允许随数据变化 |
| 导入抽屉 | Task 6 | 通过 | Task 6 | 通过 | `task3-final/import-sheet-390.png`；抽屉 390×539、bottom=844、无横向溢出；375/430 按轻量模式集中验收 |
| 解析进度 | Task 6 | 通过 | Task 6 | 通过 | `task3-final/parsing-progress-390.png`；scrollHeight=viewport=844，取消按钮 bottom=824，交易页导航隐藏 |
| 图片审核 | Task 6 | 通过 | Task 6 | 通过 | `task3-final/image-review-390.png`；350×330 主图、100px 缩略图、1:2 安全区 footer，无横向溢出 |
| 菜谱确认编辑 | Task 6 | 通过 | Task 6 | 通过 | `task3-final/recipe-confirm-390.png`；紧凑 shadcn 编辑控件、fixed footer bottom=844、无横向溢出 |
| 菜谱详情 | Task 6 | 通过 | Task 6 | 通过 | `task4-final/recipe-detail-390.png`；hero 351px、footer bottom=844、全局导航 absent、无横向溢出；收藏写入后刷新仍为 `aria-pressed=true` |
| 进入做菜指引 | Task 6 | 通过 | Task 6 | 通过 | `task5-final/cooking-guide-390.png`；全高 Drawer rect 390×844、bottom=844、无横向溢出，显式确认后才进入第 1 步 |
| 做菜模式 | Task 6 | 通过 | Task 6 | 通过 | `task5-final/cooking-mode-390.png`、`cooking-mode-restored-390.png`；footer 390×75/bottom=844，5 个动作完整可点且命中区 ≥44px，BottomNav absent、刷新恢复 3/3、无横向溢出 |
| 菜谱详情-复盘抽屉 | Task 6 | 通过 | Task 6 | 通过 | `task4-final/review-drawer-390.png`；60dvh（y338/h506）、footer y768-844，评分/改进 chips 首屏可见，5 个星级与 4 个 chip 均为 44px 命中区 |

截图目录：最终总验收使用 `output/playwright-stitch-v3/`；Task 2 收口证据位于 `output/playwright/task2-final/`；Task 3 四屏 390px 收口证据位于 `output/playwright/task3-final/`；Task 4 详情/复盘 390px 证据位于 `output/playwright/task4-final/`；Task 5 指引/做菜/恢复/复盘证据位于 `output/playwright/task5-final/`。

截图索引和像素尺寸：Task 2 共 6 张；Home/List 各为 375×844、390×844、430×844。Task 3 共 4 张、Task 4 共 2 张、Task 5 共 4 张，后三者均为 390×844；相关 375/430 在 Task 6 补齐。

Task 2 补充记录：`output/playwright-stitch-v3/task2/` 保留首轮截图；最终替换截图为 `output/playwright/task2-final/` 的 Home/List 375、390、430 六图。修正覆盖 Home 44px history target、圆形/胶囊高特异度、List 8px segmented、16px chip gap、96px 图片、点状导航指示器、Stitch 09 的 56px 白底橙色 FAB 与精确纵向间距。六图均满足 `scrollWidth === innerWidth`；390px 列表滚动到底得到 `scrollY = max = 140`，固定底栏保持 `top=750, bottom=824`，FAB 保持 `top=676, bottom=732`。浏览器控制台仅有非业务 `favicon.ico` 404。页面转场 hydration 警告经 RED→GREEN 定向用例修复后为 1/1 通过；该修正后未重复全量测试，Task 2 较早的主控全量证据仍为 17 files / 103 tests 与 build 9/9。

## 6. 真实交互验收矩阵

| 功能 | 必验行为 | 状态 | 证据 |
| --- | --- | --- | --- |
| 导入抽屉 | 打开、粘贴识别、键盘避让、拖拽关闭 | 待执行 | 待填写 |
| 解析流程 | 四阶段进度、取消、失败保留输入 | 待执行 | 待填写 |
| 图片审核 | 多选、取消、设置封面、无图保存 | 待执行 | 待填写 |
| 菜谱确认 | 标题、标签、食材、步骤可编辑并保存 | 待执行 | 待填写 |
| 搜索与分类 | 搜索、分类、筛选和返回状态恢复 | 待执行 | 待填写 |
| 收藏 | 详情、列表、做菜模式状态一致且持久化 | 部分通过 | Task 4 已验详情/列表共享控件、乐观回滚、DB 持久化及刷新保留；做菜模式一致性待 Task 5/6 |
| 做菜计时 | 增减、开始、暂停、继续和结束状态正确 | 通过（Task 5） | fake timer 覆盖绝对 deadline、暂停/继续、visibility 校准与清理；390px 真浏览器走完开始→暂停→继续→结束，刷新后 finished/00:00 恢复 |
| 步骤进度 | 完成、撤销、计数和滚动状态正确 | 通过（Task 5） | 完成/撤销定向测试；真浏览器 3 步完成、撤销第 2 步、重新完成并刷新后仍为 3/3；重复持久值不能提前解锁 |
| 语音播报 | 支持时可播报和停止；不支持时明确降级 | 通过（Task 5） | 支持检测、替换取消、切步/卸载取消与同步异常降级测试；真浏览器记录 `speak:zh-CN:<当前步骤>` 且 console/page errors 为空 |
| 复盘 | 评分、反馈、改进标签和保存结果正确 | 通过（Task 4） | 8 项 Drawer 定向行为+详情成功刷新；失败时输入保留；390px 复盘图通过 |
| 删除 | 二次确认、级联删除和列表更新正确 | 通过（Task 4 定向） | AlertDialog、成功/失败导航、sessionStorage fallback 及 DB 外键级联均有回归覆盖；Task 6 再做真浏览器总验 |

## 7. Apple 动效验收

| 检查项 | 验收要求 | 状态 | 证据 |
| --- | --- | --- | --- |
| 即时反馈 | 按下时立即反馈，不等到 click 完成 | 待执行 | 待填写 |
| 抽屉拖拽 | 内容 1:1 跟手，可中断、反向和释放归位 | 待执行 | 待填写 |
| 速度衔接 | 手势释放后弹簧继承当前速度，无跳变 | 待执行 | 待填写 |
| 导航选中态 | 共享布局动画稳定，无抖动 | 待执行 | 待填写 |
| 页面转场 | 空间关系清楚，不阻塞输入 | 待执行 | 待填写 |
| Reduced Motion | 使用淡入淡出或静态反馈替代大幅位移 | 待执行 | 待填写 |
| Reduced Transparency | 毛玻璃可降级为高不透明度表面 | 待执行 | 待填写 |

## 8. shadcn/ui 使用审计

- 实际采用的 shadcn/ui 组件：Button、Input、Textarea、Label、Checkbox、Tabs、Drawer、Dialog、AlertDialog、DropdownMenu、Skeleton、Sonner。
- 保留的业务专用组件：ImportFlow 状态协调、ImageCarousel、RecipeCard/List/Detail、FavoriteButton、CookingLogSheet；它们内部组合 shadcn 原语而不重造通用控件。
- 未使用 shadcn/ui 的例外及原因：图片、内容列表、进度时间轴和版式容器是业务表达，无对应通用 shadcn 交互原语；图标统一使用 Lucide。
- 可访问性属性检查：Task 1-5 已覆盖 44px 命中区、图标 accessible name、`aria-pressed`、Tabs/Checkbox 语义、Drawer/Dialog 焦点和 reduced-motion/transparency；Task 5 的固定计时栏五个动作在 390px 均完整可见且 ≥44px，Task 6 继续总 E2E。

## 9. 失败退回与重验记录

任何必过项失败时，按以下流程执行：

1. 将失败项标记为“失败”，保存命令输出、截图或复现步骤。
2. 当前 Task 不得标记完成，`progress.md` 不得写成通过。
3. 将失败证据、对应规格和允许修改的文件范围交给实现 agent 或专用修正 agent。
4. 修正 agent 必须运行覆盖该问题的定向测试并记录命令、结果和 commit。
5. 独立 reviewer 对修正 diff 重新进行规格与质量复审。
6. 主控重新执行失败关卡；如修正可能影响后续关卡，从受影响的最早关卡开始重跑。
7. 只有复审和主控重验均通过后，失败项才能改为“通过”。
8. 未经用户明确接受，不得把失败项降级成“已知差异”后宣布整体通过。

失败退回记录：

- Task 1 首轮复审发现 5 项 Important（reduced-motion、Vaul 抽屉降级、44px 命中目标、destructive foreground、测试弱化），退回原实现 agent，修正提交为 `416c46b`。
- Task 1 第二轮复审发现 Checkbox checked 视觉选择器 1 项 Important，再次退回，修正提交为 `d86c574`。
- Task 1 第三轮复审为 Spec PASS / Quality APPROVED，无剩余 Critical、Important 或 Minor；主控重跑 85/85 测试与 production build 后标记通过。
- Task 3 首轮复审发现 4 项 Important（provider/JSON 失败时保留安全源图、确认返回保留非首封面、V3 验证/保存/fallback 行为覆盖、解析/对话框即时反馈），退回并以 `005f30d` 关闭。
- Task 3 视觉收口后的最终复审又发现 2 项 Important（现有标签不能重命名/删除、缩略图语义与选择行为不一致），退回并以 `b02386b` 关闭。
- Task 3 最终复审为 Spec PASS / Quality APPROVED，无 Critical/Important；单行 JSX 维护性为非阻塞 Minor，按用户确认的只退回 Critical/Important 规则记录保留。
- Task 4 首轮复审发现 4 项 Important（未接线的编辑/做菜死控件、404 与可重试错误混同、V2 返回/删除回归未等价覆盖、Apple press feedback 不完整），退回并以 `fa74337` 关闭。
- Task 4 第二轮复审发现“sessionStorage key 完全不存在”的 fallback 回归尚未显式覆盖，退回并以 `4c86043` 关闭。
- Task 4 最终 build 首跑发现 `getRecipeApi` 的 Zod default 泛型可选性类型失配，按 systematic-debugging 在 API client 边界归一化后以 `ab59bce` 修复，重验 build 9/9 通过。
- Task 4 主控视觉首轮发现旧 V2 Drawer CSS 叠加导致 85dvh/标签首屏不可见及 32/36px 命中区，退回后以 `214a5ac` 收口为 60dvh 且所有评分/chip 命中区 44px。
- Task 4 最终复审为 Spec PASS / Quality APPROVED，Critical / Important / Minor 均为 None。
- Task 5 首轮复审发现 4 项 Important：storage 首屏不可用仍可能崩溃、真实 steps 加载前会破坏恢复会话、重复 completed order 可提前解锁完成、做菜页存在无行为“更多”按钮；全部在 `07b5bd7` 提交前退回原实现 agent 修正。
- Task 5 二轮复审发现新增计时结束动作使 390px footer 确定过宽；退回改为 44px shadcn 图标操作后，主控量取 `scrollWidth=innerWidth=390`、所有动作可见，关闭该项。
- Task 5 主控真浏览器首轮发现 `speechSynthesis.speak()` 同步抛错会冒泡；再次退回增加隔离 hook 安全降级与 3/3 定向测试，重验语音 `zh-CN` 调用且 errors 为空。
- Task 5 最终复审为 Spec PASS / Quality APPROVED，Critical / Important / Minor 均为 None。

## 10. 已知差异与用户接受项

待填写。没有用户明确接受记录的差异不能视为通过。

## 11. Claude Code 二次验收交接

### 11.1 获取准确代码快照

```bash
git status --short
git log --oneline -12
```

应验收的分支和 commit：待填写。

### 11.2 安装与环境

```bash
# 待填写经过验证的 Node 切换、依赖安装和环境文件步骤
```

### 11.3 Claude Code 复验命令

```bash
# 待填写可复制执行的完整测试、构建和 E2E 命令
```

### 11.4 Claude Code 人工复验入口

- 页面 URL：待填写
- 固定测试数据：待填写
- 必查流程：待填写
- 截图索引：待填写

### 11.5 Claude Code 复验结果

- 验收人或执行环境：待填写
- 验收日期：待填写
- 结果：待填写
- 新发现问题：待填写
